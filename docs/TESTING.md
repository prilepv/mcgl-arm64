# Проверки сборок

## 1.6.7 — опубликованный релиз, 6 сентября 2026

[GitHub Release v1.6.7](https://github.com/prilepv/mcgl-arm64/releases/tag/v1.6.7)
опубликован как Latest, не draft/prerelease. Исходники закреплены
в теге `v1.6.7`; последующие записи результатов
проверок в документации не меняют тег или файлы релиза.

Пакет `1.6.7-public` содержит приложение, побайтово совпадающее с проверенным
кандидатом `1.6.7-account-card-polish`. Изменились только сопровождающие документы
и упаковка DMG, поэтому контрольная сумма образа отличается от кандидатской.
Java, LWJGL, игровой runtime и патчи не пересобирались.

- `PACKAGING_CHECK_PASS`: 67 ARM64 Mach-O, deployment ≤ macOS 14,
  целостная ad-hoc подпись. Developer ID и нотариализация не добавлены.
- Итоговый DMG прошёл `hdiutil verify`; отдельный файл `.dmg.sha256`
  соответствует вычисленной контрольной сумме.
- Повторный `SOURCE_CHECK_PASS`: компиляция Swift/C/Java и все доступные
  автономные тесты, включая аккаунты, хранение паролей, вёрстку и обновлятор.
  Тесты с оригинальными клиентскими JAR пропущены: эти JAR не предоставлялись.
  Это не полный пересбор игры и не проверка игрового входа/FPS.
- Все четыре страницы сняты из настоящих AppKit-представлений в 2000×1440
  с ресурсами итогового приложения: `tools/CaptureLauncherScreenshots.swift`.
  Системный захват WindowServer ограничен только отдельным тестовым окном вне
  экрана; кэширование NSView не используется из-за артефактов полей ввода.
  Изображения проверены визуально, включая поля ввода и галочки настроек.
  Использованы изолированные настройки и вымышленные аккаунты. Журнал явно
  демонстрационный; сетевые запросы, личные профили и пароли не использовались.
- После публикации выполнена **настоящая загрузка** неизменённым обновлятором
  из `v1.6.5`: `ACCOUNTS_UPGRADE_DOWNLOAD_PASS source=1.6.5 target=1.6.7`.
  Он получил публичный `/releases/latest`, выбрал правильный DMG, скачал
  55 756 354 байта в «Загрузки» и проверил SHA-256 из GitHub asset digest.
  Независимый `shasum` скачанного файла совпал с локальным образом, GitHub
  digest и опубликованным `.dmg.sha256`. Никакой установки не выполнялось.
- `ACCOUNTS_UPGRADE_PASS mode=live-check`: неизменённый обновлятор 1.6.6
  предлагает 1.6.7; текущий обновлятор 1.6.7 считает её актуальной и не
  предлагает повторную загрузку. Файлы старых обновляторов перед компиляцией
  побайтово сверены с соответствующими Git-снимками.
- Прямые `raw.githubusercontent.com`-ссылки из тега `v1.6.7` на четыре PNG
  доступны без авторизации; их байты совпадают с локальными файлами.

SHA-256 итогового DMG:

```text
1172f9bb7fd2ece32d4e8cfdeedbf8261845f4c83bde2f3b9ebdafe84156de79
```

Предыдущие локальные кандидаты и их проверки ниже оставлены как история.

## 1.6.7 — полировка карточек и кнопок, 6 сентября 2026

Кандидат `1.6.7-account-card-polish` до подготовки публичного пакета. Его DMG
отдельно не публиковался; приложение перенесено в итоговый пакет без изменений.

- У кнопок без подписи убран ненужный отступ: карандаш и корзина рисуются
  точно по центру. Общая функция расчёта используется и отрисовкой, и тестом;
  у кнопок с текстом сохранены прежние 8 pt между значком и подписью.
- В компактную карточку на странице «Играть» возвращены аватар и именно
  игровой ник, а не локальное название карточки. Профессия под ником, уровни
  ниже; высота — 158 pt вместо 138 pt. Дополнительных кнопок управления нет.
- `SOURCE_CHECK_PASS`, `LAUNCHER_UI_PASS`, `ACCOUNTS_LAYOUT_PASS`,
  `PASSWORD_UI_PASS`, `PASSWORD_STORE_PASS` и `ACCOUNT_FORMS_PASS` повторно
  пройдены. Проверены центрирование кнопок в 30 карточках, смена ника в
  компактном блоке, видимость аватара, отсутствие перекрытия даты/действий,
  работа форм и сохранение прежнего поведения паролей.
- Превью всех четырёх страниц обновлены с ресурсами готового приложения.
  В них только вымышленные аккаунты, без личных данных и настоящих аватаров.
- `PACKAGING_CHECK_PASS`: 67 ARM64 Mach-O, deployment ≤ macOS 14,
  целостная ad-hoc подпись. Java, LWJGL, runtime, игровые патчи, фон и иконки
  приложения совпадают с базовой сборкой побайтово. Игровой вход/FPS не проверялись;
  тесты с оригинальными клиентскими JAR в этом прогоне пропущены.

DMG прошёл `hdiutil verify`. SHA-256:

```text
612e2e441149176eae12dbb0949280b9121147116a95b29592dec8f7d08803e7
```

## 1.6.7 — предыдущий локальный кандидат, 6 сентября 2026

На GitHub не опубликован. Предыдущий локальный вариант — `1.6.7-clean-accounts`:
без дублирующего выбора аккаунта на главной странице, с компактными уровнями
и сохранением пароля по явной галочке. Предыдущие кандидаты сохранены отдельно.

- `SOURCE_CHECK_PASS`: компиляция Swift/C/Java и все доступные автономные тесты.
  Игровой вход и тесты байткода, требующие оригинальных игровых JAR, не выполнялись.
- `ACCOUNTS_PASS`: миграция прежнего логина, CRUD, уникальность, лимит,
  повреждённые данные, семь профессий, границы ответа, ошибки HTTP и отсутствие
  загрузки внешних URL из HTML.
- `LAUNCHER_UI_PASS`: четыре страницы, запуск/остановка и блокировки полей,
  светлые подписи, пароль не переносится между разными аккаунтами,
  запоздалые ответы форума относятся к исходной карточке, кэш сохраняется офлайн.
- `ACCOUNTS_LAYOUT_PASS`: 30 прокручиваемых карточек, длинное имя,
  отсутствие пересечений даты/статуса с кнопками, прозрачный фон прокрутки.
  Правый блок на главной — 138 pt, без повторного логина и управления.
- `PASSWORD_STORE_PASS`: только синтетические пароли во временной папке;
  восстановление новой копией хранилища, отдельные записи аккаунтов, свежие nonce,
  привязка UUID/ника, права 700/600, обнаружение повреждений, подмены записи,
  отсутствующего ключа и ссылок. Снятие сохранения удаляет только нужную запись.
- `PASSWORD_UI_PASS`: без галочки файлы не создаются; сохранение/подстановка
  разрешены только выбранному аккаунту; в UserDefaults секретов нет.
- `ACCOUNT_FORMS_PASS`: реальные отдельные тестовые AppKit-окна добавления,
  переименования, удаления и первого согласия на локальное сохранение.
  Подтверждение/отмена, удаление пароля вместе с карточкой и поздний ответ
  удалённого аккаунта проверены без личных настроек, сети и игровой авторизации.
- С неизменённым обновлятором `v1.6.5` и 1.6.6 тестовый ответ GitHub предлагает
  1.6.7; обновлятор 1.6.7 считает её текущей. Это не скачивание опубликованного
  1.6.7: настоящий путь доставки проверяется после отдельной публикации.
- Ранее в этой работе отдельно проверен HTTPS-сервис профиля по разрешённому
  автором нику: профессия, строительство/разрушение и встроенный аватар.
  Реальный ответ не включён в репозиторий. PNG в `screenshots/1.6.7` — рендеры
  настоящих AppKit-страниц с вымышленными DemoMiner/DemoBuilder и тестовыми уровнями.
- Готовый кандидат собран на основе проверенной 1.6.6. Java, runtime-app,
  LWJGL/PortSupport, PatchTools, иконки и фон побайтово совпадают с ней.
  `PACKAGING_CHECK_PASS`: 67 Mach-O ARM64, deployment ≤ macOS 14, подпись целостна.
  DMG прошёл `hdiutil verify`; сохранённых паролей/ключей в сборке нет.

SHA-256 этого локального DMG:

```text
234516cf0a75bd7431fc4641281400086b1b3859c46604ee7e51bac583c18183
```

Это не новый FPS-замер, не проверка игрового входа с сохранённым паролем,
не аудит сетевого протокола MCGL и не сертификация для другого Mac.
Подпись остаётся ad-hoc, нотариализации Apple нет.

## 1.6.6 — разделение системной и интерфейсной иконок, 6 сентября 2026

- Боковое меню загружает `app-icon-symbol.png`; системная `app.icns` остаётся
  белой. В сборку включены оба ресурса, `CFBundleIconFile` не менялся.
- `LauncherUITest` проверяет фактически загруженный символ и его прозрачность.
  Прогон выполнен и с исходными Assets, и с ресурсами готовой `.app`.
  Отдельно просмотрен рендер реального AppKit-окна с пустыми тестовыми настройками:
  белой плашки в боковом меню нет. Пользовательский профиль не использовался.
- `SOURCE_CHECK_PASS`: компиляция и проверки без входа в игру. Тесты,
  требующие оригинальных клиентских JAR, в этом прогоне не запускались.
- Сравнение с предыдущей белой сборкой: в `.app` изменились только executable
  лаунчера, его подпись ресурсов и добавился прозрачный PNG. Системная иконка,
  всё вложенное runtime-приложение, Java, LWJGL и патчи побайтово неизменны.
- `PACKAGING_CHECK_PASS`: 67 Mach-O ARM64, deployment ≤ macOS 14, подпись целостна.
  Исправленный DMG прошёл `hdiutil verify`. Предыдущие локальные сборки сохранены.

SHA-256 актуального DMG:

```text
110c786f6d2de90f2bbaf9531c7f6ac6940a6772b1fc7c21e4d7a38d9ac2f65e
```

Версия осталась 1.6.6: уже установленная 1.6.6 не предлагает себя повторно,
поэтому повторная установка этого исправления выполняется вручную.

## 1.6.6 — предыдущее обновление белой иконки, 6 сентября 2026

Иконка собрана нативно через `ComposeAppIcon.swift` (AppKit). PNG исходного
куба и орбиты побайтово совпадает с иконкой первоначального тега `v1.6.6`;
добавлены только белая подложка и равномерные отступы.

- `ImageAssetTest --white-tile`: размеры 16, 32, 64, 128, 220, 256, 512 и 1024;
  внешние поля и скруглённые углы имеют нулевой альфа-канал, плашка белая
  и непрозрачная, центр символа непрозрачен. У 1024 PNG около 32.3% выборки
  прозрачно. Сохранённые превью совпали с повторным нативным рисованием.
- `verify-source.sh`: `SOURCE_CHECK_PASS`, компиляция Swift/C/Java, проверки UI,
  предпочтений, обновлятора, миграции и алгоритмов без запуска игры.
  Тесты с оригинальными JAR в этом прогоне не выполнялись; игровой код не менялся.
- Отдельно с исходным обновлятором из `v1.6.5`: `UPGRADE_FIXTURE_PASS` для
  обнаружения 1.6.6 и выбора DMG с SHA-256. Уже установленная 1.6.6 не должна
  предлагать саму себя повторно.
- Копия готового приложения получила новый ICNS в лаунчере и runtime-app,
  обе оболочки подписаны заново изнутри наружу. Остальные файлы `.app` совпадают
  с первоначальной сборкой. После удаления подписи на временных копиях
  исполняемый файл лаунчера совпадает целиком; у runtime отличается только
  размер VM-области `__LINKEDIT`, увеличившийся при подписи ресурсов. Остальные
  байты совпадают; исполняемый код и данные игры не изменены.
- `PACKAGING_CHECK_PASS`: 67 Mach-O ARM64, deployment target ≤ macOS 14,
  подпись целостна. DMG создан из этой копии, `hdiutil verify` прошёл.

SHA-256 обновлённого DMG:

```text
9713d9bf51c45359e0b3f156a8bad9504ff86d99edf21912aa99e84054a0d351
```

Старая локальная сборка сохранена. Тег `v1.6.6` не перемещался; новый снимок
исходников приложен к релизу отдельным архивом. Установленное приложение и
игровой профиль не заменялись в рамках этой проверки. Новых замеров FPS,
проверок игрового входа и нотариализации не было.

## 1.6.6 — первоначальный интерфейс и совместимость обновления

Дата: 2026-09-06. Среда: macOS / Apple Silicon, Zulu JDK 8u502-b07.

- Собран новый Swift-лаунчер и bootstrap-приложение с подготовленными
  зависимостями. Проверка упаковки: 67 Mach-O файлов ARM64, deployment target
  не выше macOS 14, целостность ad-hoc подписи подтверждена.
- `LauncherUpgradeTest`: неизменённый исходник обновлятора из тега `v1.6.5`
  распознаёт ответ о релизе 1.6.6, выбирает DMG и читает SHA-256 digest.
  Вариант, собранный с 1.6.6, не предлагает свою версию повторно.
- `LauncherPreferencesTest`: сохранение Xms/Xmx, FPS, VBO, профиля памяти,
  диагностики и логина из настроек 1.6.5; тестовые настройки изолированы.
- `LauncherUITest`: пять состояний единой кнопки, блокировка повторных нажатий
  в промежуточных состояниях, отсутствие действия остановки по Enter,
  переключение трёх страниц и светлый цвет всех четырёх подписей галочек.
  Журнал сохраняет нулевое горизонтальное смещение после прокрутки длинного текста.
  Этот тест создаёт только окно; игровой процесс и сеть не запускаются.
- `ImageAssetTest`: у иконки приложения есть настоящий альфа-канал, около 45.7%
  выборки пикселей прозрачно; прозрачность углов проверяется с допуском одного
  уровня 8-битного альфа-канала. Отдельная иконка GitHub/форума имеет тёмный фон.
- Вручную в собранном приложении проверены все три страницы, ⌘1/⌘2/⌘3,
  раскрытие выбора памяти, читаемость галочек и начало строк журнала.
  Сняты реальные скриншоты 1000 × 720; логин для кадра временно убран и восстановлен.
- Полный `verify-source.sh` с локальными оригинальными JAR прошёл проверки
  алгоритмов/байткода, перечисленные ниже. Оригинальные JAR не публикуются.

После публикации проверен **настоящий обновлятор установленной 1.6.5**:
он обнаружил 1.6.6 через GitHub Releases, скачал DMG, подтвердил его проверку
и открыл образ для ручной установки. SHA-256 скачанного файла совпал
с локальной сборкой и `digest` опубликованного asset:

```text
2f12a7b4d44160c1b8e154716baf6d1657d7ffcefa8021eb9aa95b618eadae27
```

Это подтверждает обнаружение и доставку обновления. Ручная замена приложения
проверяется отдельно от скачивания; запущенная игра не прерывается обновлятором.

Это не новый замер FPS и не новая проверка игры на всех моделях Mac.
Рендерер, Java runtime, протокол клиента и игровые оптимизации в 1.6.6
не менялись. Полная сборка зависимостей с нуля на чистом Mac и нотариализация
по-прежнему не проверены. Проверка макета не заменяет игровую сессию.

## Предыдущий полный прогон (1.6.5)

Дата: 2026-09-04. Среда: macOS / Apple Silicon, Zulu JDK 8u502-b07.

`tools/verify-source.sh` завершился с `SOURCE_CHECK_PASS` и кодом выхода 0.
Использовались исходники из этого репозитория, ASM/LWJGL из приложения 1.6.4
и локальные оригинальные JAR MCGL. Оригинальные файлы в репозиторий не включены.

Проверено:

- Компиляция Swift-лаунчера, native runtime, Cocoa JNI-моста и двух C-библиотек.
- Разбор версии GitHub Release, выбор DMG и сравнение семантических версий.
- Одноразовая миграция `alphaSort:false` с сохранением LF/CRLF.
- Компиляция Java-части LWJGL overlay, DirectLauncher, патчеров, helpers и тестов.
- 100 000 операций списка частиц и 20 000 сравнений записи пикселей атласа.
- 100 000 операций индекса геометрии.
- Все шесть ограничений FPS, паузы, некорректные значения и переполнение таймера.
- Диагностика кадров и VBO при включённых/выключенных настройках.
- Сохранение GLuint-идентификаторов текстур и старого пути при отключении VBO.
- 5 402 сравнения сортировки с оригинальным байткодом, включая равные расстояния,
  NaN/Infinity и многопоточные тестовые вызовы.
- Защитные проверки патчеров: изменённые методы, повторное применение,
  неподдерживаемое состояние текстуры.
- Включение штатной сортировки прозрачных блоков только после проверки поля,
  метода настройки и фактического использования флага рендерером чанков.
- Анимации: 2 000 кадров, совпадение пикселей и порядка тиков/привязок.
- Освещение: 5 000 кадров, совпадение пикселей и побочных GL-состояний;
  953 передачи вместо 5 000 в тестовом сценарии. С отключённым кешем — 5 000.

Это детерминированные проверки алгоритмов и байткода с имитацией GL-вызовов.
Числа из микробенчмарков **не являются FPS игры**. Этот прогон не запускает
игру, не подключается к серверу и не проверяет реальные окна, GPU, Gatekeeper
или поведение на другом Mac. Полная независимая сборка DMG с нуля не проверена.

Компилятор сообщает предупреждения об устаревших OpenGL/MD5 API; они не были
скрыты или исправлены при публикации снимка.
